---
"@lystran/pi-guard": minor
---

Send a terminal notification right before a confirmation dialog appears, using `OSC 99` on kitty and `OSC 777` on the other terminals that render it, with tmux and GNU screen excluded, and the terminal bell now written only on an interactive terminal
