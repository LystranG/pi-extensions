---
"@lystran/pi-session-rename": patch
---

Name a session started with a prompt template or `/skill:<name>` from the arguments the user passed to the command instead of the expanded template or skill body, which Pi substitutes into the prompt before the extension sees it
