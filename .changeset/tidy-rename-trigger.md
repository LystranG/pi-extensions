---
"@lystran/pi-session-rename": patch
---

Name a session from the user's own text after Pi expands `/skill:<name>` and prompt templates, and warn when the title provider returns an error instead of failing silently
